<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:version='2' expr:dir='data:blog.languageDirection' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
  <meta charset='utf-8'/>
  <title><data:blog.pageTitle/></title>
  <meta content='width=device-width, initial-scale=1' name='viewport'/>

  <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet'/>
  <link href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css' rel='stylesheet'/>
  <link href='https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800;900&amp;display=swap' rel='stylesheet'/>

  <b:skin><![CDATA[
    /* ---------------------------------------------------------
       1. CORE RESET & LAYER ARCHITECTURE
    ------------------------------------------------------------ */
    :root {
      --fe-primary: #000000;
      --fe-accent: #ffc107;
      --fe-danger: #ff4d4f;
      --fe-success: #25D366;
      --fe-white: #ffffff;
      --fe-mist: #f4f4f4;
      --fe-border: #eeeeee;
      --fe-radius-lg: 24px;
      --fe-transition: 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
    }

    * { box-sizing: border-box; outline: none !important; }
    
    /* Global Icon Protection */
    i.bi { display: inline-block !important; pointer-events: none !important; min-width: 1em; line-height: 1; }
    
    body { 
      margin: 0; padding-top: 80px; 
      font-family: 'Poppins', sans-serif; 
      background: #fff; color: var(--fe-primary); 
      overflow-x: hidden; 
    }

    /* Blogger Cleanse: No system widgets allowed to block clicks */
    .header, .blog-feeds, #navbar-iframe-container, .post-footer, .comments, .feed-links, .widget, .status-msg-wrap { 
      display: none !important; visibility: hidden !important; height: 0 !important; opacity: 0 !important; 
    }

    /* ---------------------------------------------------------
       2. Z-INDEX SYSTEM (The Iron Shield)
    ------------------------------------------------------------ */
    .fe-navbar { z-index: 10000 !important; }
    .side-backdrop { z-index: 20000 !important; }
    .fe-sidebar { z-index: 30000 !important; }
    .fe-modal-backdrop { z-index: 40000 !important; }
    .fe-search-overlay { z-index: 50000 !important; }

    /* ---------------------------------------------------------
       3. NAVIGATION
    ------------------------------------------------------------ */
    .fe-navbar { position: fixed; top: 0; left: 0; width: 100%; height: 80px; background: rgba(255,255,255,0.98); backdrop-filter: blur(10px); border-bottom: 1px solid var(--fe-border); display: flex; align-items: center; }
    .nav-inner { display: flex; justify-content: space-between; align-items: center; width: 100%; padding: 0 4%; }
    .fe-logo { font-weight: 900; font-size: 1.6rem; color: #000; text-decoration: none; letter-spacing: -1.5px; }
    .fe-logo span { color: var(--fe-accent); }
    
    .nav-actions { display: flex; align-items: center; gap: 12px; }
    .fe-icon { position: relative; cursor: pointer; font-size: 1.5rem; width: 45px; height: 45px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: transparent; transition: 0.3s; color: #000; }
    .fe-icon:hover { background: var(--fe-mist); }
    .fe-badge { position: absolute; top: 2px; right: 2px; background: var(--fe-danger); color: #fff; font-size: 10px; font-weight: 900; height: 18px; width: 18px; border-radius: 50%; display: flex; align-items: center; justify-content: center; border: 2px solid #fff; }

    /* ---------------------------------------------------------
       4. HERO & PRODUCT GRID
    ------------------------------------------------------------ */
    .fe-hero { height: 45vh; position: relative; display: flex; align-items: center; justify-content: center; color: white; text-align: center; }
    .hero-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1579871494447-9811cf80d66c?q=80&w=2000'); background-size: cover; z-index: 1; }
    .hero-content { position: relative; z-index: 2; }
    .reveal { opacity: 0; transform: translateY(30px); transition: 0.8s var(--fe-transition); }
    .reveal.active { opacity: 1; transform: translateY(0); }

    .fe-product-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 20px; padding: 40px 0; }
    .fe-card { background: white; border-radius: var(--fe-radius-lg); border: 1px solid var(--fe-border); overflow: hidden; position: relative; cursor: pointer; display: flex; flex-direction: column; }
    .card-img-box { height: 250px; overflow: hidden; position: relative; }
    .card-img-box img { width: 100%; height: 100%; object-fit: cover; }
    
    .card-actions { position: absolute; bottom: 12px; left: 12px; right: 12px; display: flex; gap: 8px; z-index: 10; transition: 0.4s var(--fe-transition); }
    .grid-btn { height: 42px; border-radius: 50px; border: none; font-weight: 700; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: 0.3s; }
    .grid-btn-cart { background: var(--fe-primary); color: #fff; flex: 1; gap: 8px; font-size: 0.85rem; }
    .grid-btn-wish { background: rgba(255,255,255,0.9); backdrop-filter: blur(5px); width: 42px; color: #ccc; border: 1px solid #eee; }
    .grid-btn-wish.active { color: var(--fe-danger); border-color: var(--fe-danger); }

    /* ---------------------------------------------------------
       5. SIDEBARS (Basket & Saved)
    ------------------------------------------------------------ */
    .side-backdrop { 
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; 
      background: rgba(0,0,0,0.6); backdrop-filter: blur(8px); 
      display: none; transition: 0.3s;
    }

    .fe-sidebar { 
      position: fixed; top: 0; right: -110%; 
      width: 100%; max-width: 420px; height: 100vh; 
      background: #ffffff !important; 
      transition: right 0.5s var(--fe-transition); 
      display: flex; flex-direction: column; 
      box-shadow: -20px 0 60px rgba(0,0,0,0.2); 
      visibility: hidden;
    }
    .fe-sidebar.open { right: 0 !important; visibility: visible !important; }

    .side-head { padding: 25px; background: #000; color: #fff; display: flex; justify-content: space-between; align-items: center; }
    .side-body { flex-grow: 1; overflow-y: auto; padding: 20px; background: #fff; }
    .row-item { display: flex; gap: 15px; align-items: center; padding-bottom: 15px; margin-bottom: 15px; border-bottom: 1px solid var(--fe-border); position: relative; }
    .row-item img { width: 60px; height: 60px; border-radius: 12px; object-fit: cover; }
    
    .js-del-cart, .js-del-wish { 
      cursor: pointer !important; padding: 5px 12px; background: #fff5f5; color: var(--fe-danger); 
      border-radius: 50px; font-size: 0.7rem; font-weight: 900; 
    }

    /* ---------------------------------------------------------
       6. QUICK VIEW & SEARCH
    ------------------------------------------------------------ */
    .fe-modal-backdrop { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); display: none; align-items: center; justify-content: center; padding: 15px; }
    .fe-modal-inner { background: #fff; width: 100%; max-width: 950px; border-radius: 24px; position: relative; display: flex; flex-direction: row; overflow: hidden; box-shadow: 0 30px 60px rgba(0,0,0,0.4); }
    .fe-modal-x { position: absolute; top: 20px; right: 20px; width: 45px; height: 45px; background: #000; color: #fff; display: flex; align-items: center; justify-content: center; border-radius: 50%; cursor: pointer; z-index: 40005; border: 2px solid #fff; }
    
    .qv-left { flex: 1.2; background: #f9f9f9; padding: 20px; display: flex; align-items: center; justify-content: center; }
    .qv-left img { width: 100%; border-radius: 15px; }
    .qv-right { flex: 1; padding: 40px; display: flex; flex-direction: column; justify-content: center; }
    .qv-qty-input { display: flex; align-items: center; background: #f5f5f5; border-radius: 50px; padding: 10px 20px; gap: 20px; width: fit-content; margin-bottom: 25px; }

    .fe-search-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100vh; background: #fff; display: none; flex-direction: column; align-items: center; padding-top: 80px; }
    .fe-search-input { width: 90%; max-width: 600px; border: none; border-bottom: 3px solid #000; font-size: 2.5rem; font-weight: 800; text-align: center; outline: none; }
    .search-results { width: 90%; max-width: 600px; max-height: 60vh; overflow-y: auto; margin-top: 30px; }
    .search-item { display: flex; align-items: center; gap: 20px; padding: 15px; background: var(--fe-mist); border-radius: 15px; cursor: pointer; transition: 0.3s; margin-bottom: 10px; text-align: left; }

    @keyframes temuPop { from { transform: scale(0.9); opacity: 0; } to { transform: scale(1); opacity: 1; } }

    @media (max-width: 850px) {
      .fe-modal-inner { flex-direction: column; max-height: 95vh; overflow-y: auto; }
      .fe-product-grid { grid-template-columns: repeat(2, 1fr); gap: 10px; }
      .fe-sidebar { max-width: 90%; }
      .qv-right { padding: 25px; }
    }
  ]]></b:skin>
</head>

<body>
  <nav class='fe-navbar'>
    <div class='nav-inner'>
      <a class='fe-logo' href='#hero'>EVENT<span>SUSHI</span></a>
      <div class='nav-actions'>
        <div class='fe-icon' id='btnOpenSearch'><i class='bi bi-search'>&#160;</i></div>
        <div class='fe-icon' id='wishToggle'><i class='bi bi-heart'>&#160;</i><span class='fe-badge' id='wishBadge'>0</span></div>
        <div class='fe-icon' id='cartToggle'><i class='bi bi-bag'>&#160;</i><span class='fe-badge' id='cartBadge'>0</span></div>
      </div>
    </div>
  </nav>

  <section class='fe-hero' id='hero'>
    <div class='hero-bg'>&#160;</div>
    <div class='hero-content'><h1 class='reveal'>Handcrafted Fresh.</h1><p class='reveal mb-4'>The best sushi experience in Casablanca.</p></div>
  </section>

  <div class='container'><div class='fe-product-grid' id='feMenuGrid'>&#160;</div></div>

  <div class='side-backdrop js-close-all' id='feSideBackdrop'>&#160;</div>
  
  <aside class='fe-sidebar' id='sideCartPanel'>
    <div class='side-head'><h5 class='m-0 fw-bold'>Basket</h5><div class='fe-icon js-close-all' style='color:#fff'><i class='bi bi-x-lg'>&#160;</i></div></div>
    <div class='side-body js-cart-list' id='feCartList'>&#160;</div>
    <div class='side-foot' id='feCartFooter' style='display:none'>
        <div class='h6 fw-bold d-flex justify-content-between mb-3'><span>Total:</span><span id='feFinalTotalDisplay'>0 DH</span></div>
        <button class='btn btn-dark w-100 py-3 rounded-pill fw-bold' id='btnOrderWA'>ORDER VIA WHATSAPP</button>
    </div>
  </aside>

  <aside class='fe-sidebar' id='sideWishPanel'>
    <div class='side-head' style='background:var(--fe-danger)'><h5 class='m-0 fw-bold'>Saved Items</h5><div class='fe-icon js-close-all' style='color:#fff'><i class='bi bi-x-lg'>&#160;</i></div></div>
    <div class='side-body js-wish-list' id='feWishList'>&#160;</div>
  </aside>

  <div class='fe-modal-backdrop' id='modalQuickView'>
    <div class='fe-modal-x js-close-all'><i class='bi bi-x-lg'>&#160;</i></div>
    <div class='fe-modal-inner' onclick='event.stopPropagation()'>
      <div class='qv-left' style='max-width:400px'><img alt='Product' id='feQVImg' src=''/></div>
      <div class='qv-right'>
        <h2 class='fw-bold' id='feQVTitle'>-</h2>
        <div class='h3 text-danger mb-3' id='feQVPrice'>-</div>
        <p class='text-muted small' id='feQVDesc'>-</p>
        <div class='qv-qty-input'>
          <span class='js-modal-qty' data-op='-' style='cursor:pointer; font-weight:900'>−</span>
          <span class='fw-bold' id='feQVQtyVal'>1</span>
          <span class='js-modal-qty' data-op='+' style='cursor:pointer; font-weight:900'>+</span>
        </div>
        <div class='d-flex gap-2'>
           <button class='btn btn-dark py-3 flex-grow-1 rounded-pill fw-bold' id='btnModalAddToCart'>Add to Basket</button>
           <button class='grid-btn-wish js-wish-toggle' id='btnModalWishlist' style='width:55px; height:55px; border-radius:50%'><i class='bi bi-heart-fill'>&#160;</i></button>
        </div>
      </div>
    </div>
  </div>

  <div class='fe-search-overlay' id='searchOverlay'>
    <div class='fe-modal-x js-close-all'><i class='bi bi-x-lg'>&#160;</i></div>
    <input class='fe-search-input' id='feSearchIn' placeholder='Find sushi...' type='text'/>
    <div class='search-results container' id='feSearchResults'>&#160;</div>
  </div>

  <b:section id='BloggerLogic' showaddelement='yes'/>

  <script type='text/javascript'>
  //<![CDATA[
  document.addEventListener("DOMContentLoaded", function() {
    (function(){
      const FE = {
        config: { waPhone: "212600000000", storeName: "EVENT SUSHI" },
        catalog: [
          { id: "p1", title: "Salmon Lovers Box", price: 120, img: "https://images.unsplash.com/photo-1579871494447-9811cf80d66c?w=600", desc: "12 pieces of premium fresh salmon selection." },
          { id: "p2", title: "Dragon Platter", price: 155, img: "https://images.unsplash.com/photo-1553621042-f6e147245754?w=600", desc: "Tempura shrimp roll topped with grilled unagi." },
          { id: "p3", title: "Spicy Tuna Taco", price: 45, img: "https://images.unsplash.com/photo-1593560708920-61dd98c46a4e?w=600", desc: "Crunchy nori shell filled with spicy marinated tuna." }
        ],
        state: { cart: JSON.parse(localStorage.getItem("fe_cart") || "[]"), wish: JSON.parse(localStorage.getItem("fe_wish") || "[]"), qvId: null, qvQty: 1 },
        $: id => document.getElementById(id),
        $$: s => document.querySelectorAll(s),
        save: function() { localStorage.setItem("fe_cart",JSON.stringify(this.state.cart)); localStorage.setItem("fe_wish",JSON.stringify(this.state.wish)); },
        
        closeAll: function() {
          this.$$(".fe-sidebar").forEach(s => s.classList.remove("open"));
          this.$$(".fe-modal-backdrop, .fe-search-overlay").forEach(m => m.style.display = "none");
          this.$("feSideBackdrop").style.display = "none";
        },

        renderMenu: function() {
          this.$("feMenuGrid").innerHTML = this.catalog.map(p => `
            <div class="fe-card reveal js-card" data-id="${p.id}">
              <div class="card-img-box"><img src="${p.img}"/><div class="card-actions">
                  <button class="grid-btn grid-btn-cart js-quick-add" data-id="${p.id}">Add</button>
                  <button class="grid-btn grid-btn-wish js-wish-toggle ${this.state.wish.includes(p.id)?'active':''}" data-id="${p.id}"><i class="bi bi-heart-fill">&#160;</i></button>
              </div></div>
              <div class="card-body"><h6 class="fw-bold">${p.title}</h6><div>${p.price} DH</div></div>
            </div>`).join('');
          const obs = new IntersectionObserver(es => es.forEach(e => { if(e.isIntersecting) e.target.classList.add("active"); }), {threshold: 0.1});
          this.$$(".reveal").forEach(el => obs.observe(el));
        },

        renderCart: function() {
          const valid = this.state.cart.filter(i => this.catalog.find(p => p.id === i.id));
          this.$("feCartList").innerHTML = valid.map(i => {
            let p = this.catalog.find(x => x.id === i.id);
            return `<div class="row-item"><img src="${p.img}"/><div><h6>${p.title}</h6><small>${p.price} DH x ${i.qty}</small></div><div class="ms-auto js-del-cart" data-id="${i.id}">DEL</div></div>`;
          }).join('');
          this.$("feFinalTotalDisplay").textContent = valid.reduce((s,i) => s + (this.catalog.find(x => x.id === i.id).price * i.qty), 0) + " DH";
          this.$("feCartFooter").style.display = valid.length ? "block" : "none";
          this.$("cartBadge").textContent = valid.reduce((s,i) => s + i.qty, 0);
          this.save();
        },

        renderWish: function() {
          const valid = this.state.wish.filter(id => this.catalog.find(p => p.id === id));
          this.$("feWishList").innerHTML = valid.map(id => {
            let p = this.catalog.find(x => x.id === id);
            return `<div class="row-item"><img src="${p.img}"/><div><h6>${p.title}</h6><button class="btn btn-sm btn-dark js-quick-add" data-id="${p.id}">+ Basket</button></div><div class="ms-auto js-del-wish" data-id="${id}">X</div></div>`;
          }).join('');
          this.$("wishBadge").textContent = valid.length;
          this.save();
        },

        bindEvents: function() {
          const self = this;
          document.addEventListener("click", e => {
            const t = e.target;
            
            if (t.closest("#cartToggle")) { self.$("sideCartPanel").classList.add("open"); self.$("feSideBackdrop").style.display = "block"; return; }
            if (t.closest("#wishToggle")) { self.$("sideWishPanel").classList.add("open"); self.$("feSideBackdrop").style.display = "block"; return; }
            if (t.closest("#btnOpenSearch")) { self.$("searchOverlay").style.display = "flex"; self.$("feSearchIn").focus(); return; }
            if (t.closest(".js-close-all") || t.id === "feSideBackdrop" || t.id === "modalQuickView") { self.closeAll(); return; }
            
            if (t.closest(".js-del-cart")) { self.state.cart = self.state.cart.filter(x => x.id !== t.closest(".js-del-cart").dataset.id); self.renderCart(); return; }
            if (t.closest(".js-del-wish")) { self.state.wish = self.state.wish.filter(x => x !== t.closest(".js-del-wish").dataset.id); self.renderWish(); self.renderMenu(); return; }

            const card = t.closest(".js-card") || t.closest(".search-item");
            if (card && !t.closest(".card-actions")) {
              const p = self.catalog.find(x => x.id === card.dataset.id);
              self.state.qvId = p.id; self.state.qvQty = 1;
              self.$("feQVImg").src = p.img; self.$("feQVTitle").textContent = p.title; self.$("feQVPrice").textContent = p.price + " DH"; self.$("feQVDesc").textContent = p.desc; self.$("feQVQtyVal").textContent = "1";
              self.$("btnModalWishlist").classList.toggle("active", self.state.wish.includes(p.id));
              self.$("searchOverlay").style.display = "none";
              self.$("modalQuickView").style.display = "flex";
              return;
            }

            if (t.closest(".js-modal-qty")) {
              const op = t.closest(".js-modal-qty").dataset.op;
              op === "+" ? self.state.qvQty++ : (self.state.qvQty > 1 ? self.state.qvQty-- : null);
              self.$("feQVQtyVal").textContent = self.state.qvQty;
              return;
            }

            if (t.id === "btnModalAddToCart" || t.closest(".js-quick-add")) {
              const btn = t.closest(".js-quick-add");
              const id = btn ? btn.dataset.id : self.state.qvId;
              const qty = btn ? 1 : self.state.qvQty;
              const ex = self.state.cart.find(x => x.id === id);
              ex ? ex.qty += qty : self.state.cart.push({id, qty});
              self.closeAll(); self.renderCart(); self.$("sideCartPanel").classList.add("open"); self.$("feSideBackdrop").style.display = "block";
              return;
            }

            if (t.closest(".js-wish-toggle")) {
              const id = t.closest(".js-wish-toggle").dataset.id || self.state.qvId;
              const idx = self.state.wish.indexOf(id);
              idx > -1 ? self.state.wish.splice(idx, 1) : self.state.wish.push(id);
              self.renderWish(); self.renderMenu();
              if(self.$("btnModalWishlist")) self.$("btnModalWishlist").classList.toggle("active", self.state.wish.includes(self.state.qvId));
              return;
            }

            if (t.id === "btnOrderWA") {
                let msg = `🍣 ORDER: ${self.config.storeName}\n------------------\n`;
                self.state.cart.forEach(item => {
                    let p = self.catalog.find(x => x.id === item.id);
                    msg += `• ${item.qty}x ${p.title} (${p.price * item.qty} DH)\n`;
                });
                window.open(`https://wa.me/${self.config.waPhone}?text=${encodeURIComponent(msg)}`);
            }
          }, true);

          this.$("feSearchIn").onkeyup = e => {
            const v = e.target.value.toLowerCase().trim();
            if(v.length < 2) { self.$("feSearchResults").innerHTML = ""; return; }
            self.$("feSearchResults").innerHTML = self.catalog.filter(p => p.title.toLowerCase().includes(v)).map(p => `<div class="search-item js-card" data-id="${p.id}"><img src="${p.img}"/><div><h6>${p.title}</h6><small>${p.price} DH</small></div></div>`).join('');
          };
        },
        init: function() { this.renderMenu(); this.renderCart(); this.renderWish(); this.bindEvents(); }
      };
      FE.init();
    })();
  });
  //]]>
  </script>
</body>
</html>
