<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:version='2' expr:dir='data:blog.languageDirection' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
  <meta charset='utf-8'/>
  <title><data:blog.pageTitle/></title>
  <meta content='width=device-width, initial-scale=1' name='viewport'/>

  <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet'/>
  <link href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css' rel='stylesheet'/>
  <link href='https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800;900&amp;display=swap' rel='stylesheet'/>

  <b:skin><![CDATA[
    /* ---------------------------------------------------------
       MODULE 1: DESIGN TOKENS & RESET
    ------------------------------------------------------------ */
    :root {
      --fe-primary: #000000;
      --fe-accent: #ffc107;
      --fe-danger: #ff4d4f;
      --fe-success: #25D366;
      --fe-white: #ffffff;
      --fe-mist: #f4f4f4;
      --fe-border: #eeeeee;
      --fe-radius: 20px;
      --fe-transition: 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    }

    * { box-sizing: border-box; outline: none !important; }
    i.bi { display: inline-block !important; pointer-events: none !important; min-width: 1em; line-height: 1; }
    body { margin: 0; padding-top: 140px; font-family: 'Poppins', sans-serif; background: #fff; color: var(--fe-primary); overflow-x: hidden; }

    /* Blogger Cleanup */
    .header, .blog-feeds, #navbar-iframe-container, .post-footer, .comments, .widget, .status-msg-wrap { 
      display: none !important; visibility: hidden !important; height: 0 !important; opacity: 0 !important; 
    }

    /* ---------------------------------------------------------
       Z-INDEX ARCHITECTURE (The Iron Shield)
    ------------------------------------------------------------ */
    .fe-navbar { z-index: 10000 !important; }
    .fe-cat-bar { z-index: 9500 !important; }
    .side-backdrop { z-index: 20000 !important; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.6); backdrop-filter: blur(8px); display: none; opacity: 0; transition: 0.3s; pointer-events: auto; }
    .fe-sidebar { z-index: 30000 !important; position: fixed; top: 0; right: -110%; width: 100%; max-width: 450px; height: 100vh; background: #ffffff !important; transition: right 0.5s var(--fe-transition); display: flex; flex-direction: column; box-shadow: -20px 0 60px rgba(0,0,0,0.2); visibility: hidden; }
    .fe-modal-backdrop { z-index: 40000 !important; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); display: none; align-items: center; justify-content: center; padding: 15px; }
    .fe-search-overlay { z-index: 50000 !important; position: fixed; top: 0; left: 0; width: 100%; height: 100vh; background: #fff; display: none; padding-top: 80px; }

    /* ---------------------------------------------------------
       UI COMPONENTS: NAVBAR, CAT BAR & HERO
    ------------------------------------------------------------ */
    .fe-navbar { position: fixed; top: 0; left: 0; width: 100%; height: 80px; background: rgba(255,255,255,0.98); border-bottom: 1px solid var(--fe-border); display: flex; align-items: center; }
    .nav-inner { display: flex; justify-content: space-between; align-items: center; width: 100%; padding: 0 4%; }
    .fe-logo { font-weight: 900; font-size: 1.6rem; color: #000; text-decoration: none; letter-spacing: -1px; }
    .fe-logo span { color: var(--fe-accent); }
    .nav-actions { display: flex; align-items: center; gap: 12px; }
    .fe-icon { position: relative; cursor: pointer; font-size: 1.4rem; width: 45px; height: 45px; display: flex; align-items: center; justify-content: center; border-radius: 50%; color: #000; transition: 0.3s; }
    .fe-icon:hover { background: var(--fe-mist); }
    .fe-badge { position: absolute; top: 2px; right: 2px; background: var(--fe-danger); color: #fff; font-size: 10px; font-weight: 900; height: 18px; width: 18px; border-radius: 50%; display: flex; align-items: center; justify-content: center; border: 2px solid #fff; }

    .fe-cat-bar { position: fixed; top: 80px; left: 0; width: 100%; height: 60px; background: #fff; border-bottom: 1px solid var(--fe-border); display: flex; align-items: center; overflow-x: auto; white-space: nowrap; padding: 0 4%; scrollbar-width: none; }
    .cat-item { display: inline-block; padding: 8px 22px; margin-right: 12px; border-radius: 50px; background: var(--fe-mist); font-size: 0.85rem; font-weight: 700; cursor: pointer; transition: 0.3s; }
    .cat-item.active { background: #000; color: #fff; }

    .fe-hero { height: 45vh; position: relative; display: flex; align-items: center; justify-content: center; color: white; text-align: center; margin-top: -60px; margin-bottom: 20px; overflow: hidden; }
    .hero-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1579871494447-9811cf80d66c?q=80&amp;w=2000'); background-size: cover; background-position: center; z-index: 1; }
    .hero-content { position: relative; z-index: 2; }
    .hero-content h1 { font-size: clamp(2.2rem, 6vw, 4rem); font-weight: 900; line-height: 1; margin-bottom: 15px; }

    /* ---------------------------------------------------------
       GRID & CARDS
    ------------------------------------------------------------ */
    .fe-product-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 25px; padding: 40px 0; width: 100%; margin: 0 auto; justify-content: start; }
    .fe-card { background: white; border-radius: var(--fe-radius); border: 1px solid var(--fe-border); overflow: hidden; position: relative; cursor: pointer; display: flex; flex-direction: column; transition: 0.3s; height: 100%; }
    .fe-card:hover { transform: translateY(-8px); box-shadow: 0 15px 30px rgba(0,0,0,0.05); }

    .card-img-box { width: 100%; aspect-ratio: 1/1; overflow: hidden; position: relative; background: #f9f9f9; }
    .card-img-box img { width: 100%; height: 100%; object-fit: cover; transition: 0.5s; }
    .fe-card:hover img { transform: scale(1.08); }

    .card-actions { position: absolute; bottom: 12px; left: 12px; right: 12px; display: flex; gap: 8px; z-index: 10; }
    .grid-btn { height: 42px; border-radius: 50px; border: none; font-weight: 700; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: 0.3s; }
    .grid-btn-cart { background: var(--fe-primary); color: #fff; flex: 1; font-size: 0.85rem; }
    .grid-btn-wish { background: rgba(255,255,255,0.9); backdrop-filter: blur(5px); width: 42px; color: #ccc; border: 1px solid #eee; }
    .grid-btn-wish.active { color: var(--fe-danger); border-color: var(--fe-danger); }

    .product-label { position: absolute; top: 12px; left: 12px; padding: 4px 12px; border-radius: 8px; font-size: 0.65rem; font-weight: 900; text-transform: uppercase; z-index: 5; color: #fff; }
    .label-hot { background: #ff4d4f; } .label-new { background: #25D366; } .label-sale { background: #ffc107; color: #000; }
    .flash-timer { position: absolute; top: 12px; right: 12px; background: rgba(0,0,0,0.7); color: #fff; padding: 4px 10px; border-radius: 8px; font-size: 0.7rem; font-weight: 800; z-index: 5; }

    /* ---------------------------------------------------------
       QUICKVIEW: GALLERY & ARROWS (Step 14)
    ------------------------------------------------------------ */
    .fe-modal-inner { background: #fff; width: 100%; max-width: 950px; border-radius: 32px; position: relative; display: flex; flex-direction: row; overflow: hidden; box-shadow: 0 40px 100px rgba(0,0,0,0.5); max-height: 90vh; }
    .fe-modal-x { position: absolute; top: 15px; right: 15px; width: 45px; height: 45px; background: #000; color: #fff; display: flex; align-items: center; justify-content: center; border-radius: 50%; cursor: pointer; z-index: 40005; border: 2px solid #fff; }
    
    .qv-left { flex: 1.2; background: #fdfdfd; display: flex; flex-direction: column; align-items: center; padding: 25px; border-right: 1px solid #f0f0f0; gap: 20px; position: relative; }
    .qv-main-container { width: 100%; position: relative; display: flex; align-items: center; justify-content: center; }
    .qv-main-img { width: 100%; aspect-ratio: 1/1; object-fit: contain; border-radius: 15px; background: #fff; }
    
    /* Navigation Arrows */
    .qv-nav-btn { position: absolute; top: 50%; transform: translateY(-50%); width: 45px; height: 45px; background: rgba(255,255,255,0.9); border-radius: 50%; border: 1px solid #eee; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: 0.3s; z-index: 10; box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
    .qv-nav-btn:hover { background: #000; color: #fff; }
    .qv-nav-prev { left: 10px; }
    .qv-nav-next { right: 10px; }

    .qv-thumb-grid { display: flex; gap: 10px; overflow-x: auto; width: 100%; padding: 5px 0; scrollbar-width: none; justify-content: center; }
    .qv-thumb { width: 60px; height: 60px; border-radius: 12px; object-fit: cover; cursor: pointer; border: 2px solid transparent; opacity: 0.5; transition: 0.2s; }
    .qv-thumb.active { border-color: #000; opacity: 1; transform: scale(1.05); }

    .qv-right { flex: 1; padding: 45px; display: flex; flex-direction: column; justify-content: center; }
    .qv-title { font-size: 2rem; font-weight: 800; line-height: 1.2; margin-bottom: 10px; }
    .qv-price-row { display: flex; align-items: center; gap: 15px; margin-bottom: 25px; }
    .qv-price { font-size: 2.2rem; font-weight: 900; color: #000; }
    .qv-old-price { text-decoration: line-through; color: #aaa; font-size: 1.1rem; }
    .qv-qty-input { display: flex; align-items: center; background: #f4f4f4; padding: 12px 25px; border-radius: 100px; width: fit-content; gap: 25px; margin-bottom: 30px; }
    .qty-btn { cursor: pointer; font-size: 1.4rem; font-weight: 800; user-select: none; }

    /* ---------------------------------------------------------
       SIDEBARS & BASKET FORM (Step 10)
    ------------------------------------------------------------ */
    .fe-sidebar.open { right: 0 !important; visibility: visible !important; }
    .side-head { padding: 25px; background: #000; color: #fff; display: flex; justify-content: space-between; align-items: center; }
    .side-body { flex-grow: 1; overflow-y: auto; padding: 25px; background: #fff; }
    .fe-input { width: 100%; padding: 15px; border-radius: 15px; border: 1px solid #ddd; margin-bottom: 12px; font-size: 0.95rem; transition: 0.3s; }
    .fe-input:focus { border-color: #000; box-shadow: 0 0 0 4px rgba(0,0,0,0.05); }
    .row-item { display: flex; gap: 15px; align-items: center; padding-bottom: 15px; margin-bottom: 15px; border-bottom: 1px solid var(--fe-border); }
    .row-item img { width: 60px; height: 60px; border-radius: 12px; object-fit: cover; }
    .js-del-cart { cursor: pointer; color: var(--fe-danger); font-weight: 900; font-size: 0.75rem; background: #fff5f5; padding: 6px 12px; border-radius: 50px; }

    @media (max-width: 850px) {
      .fe-modal-inner { flex-direction: column; overflow-y: auto; width: 94%; }
      .qv-left { border-right: none; border-bottom: 1px solid #eee; padding: 20px; }
      .qv-right { padding: 30px; }
      .fe-sidebar { max-width: 95%; }
      .fe-product-grid { grid-template-columns: repeat(2, 1fr); gap: 15px; }
    }
  ]]></b:skin>
</head>

<body>
  <nav class='fe-navbar'>
    <div class='nav-inner'>
      <a class='fe-logo' href='#hero'>GORARY<span>SUSHI</span></a>
      <div class='nav-actions'>
        <div class='fe-icon' id='wishToggle'><i class='bi bi-heart'>&#160;</i><span class='fe-badge' id='wishBadge'>0</span></div>
        <div class='fe-icon' id='cartToggle'><i class='bi bi-bag'>&#160;</i><span class='fe-badge' id='cartBadge'>0</span></div>
      </div>
    </div>
  </nav>

  <div class='fe-cat-bar' id='feCatBar'>
      <div class='cat-item active' data-cat='all'>All Menu</div>
      <div class='cat-item' data-cat='Special Rolls'>Special Rolls</div>
      <div class='cat-item' data-cat='Nigiri'>Nigiri</div>
      <div class='cat-item' data-cat='Sides'>Sides</div>
  </div>

  <section class='fe-hero' id='hero'>
    <div class='hero-bg'>&#160;</div>
    <div class='hero-content'>
      <h1 class='reveal'>Art on a Plate.</h1>
      <p class='reveal'>Experience Casablanca&#39;s Finest Sushi Selection</p>
    </div>
  </section>

  <div class='container'>
      <div class='fe-product-grid' id='feMenuGrid'>&#160;</div>
  </div>

  <div class='side-backdrop js-close-all' id='feSideBackdrop'>&#160;</div>
  
  <aside class='fe-sidebar' id='sideCartPanel'>
    <div class='side-head'><h5 class='m-0 fw-bold'>Basket</h5><div class='fe-icon js-close-all' style='color:#fff'><i class='bi bi-x-lg'>&#160;</i></div></div>
    <div class='side-body'>
      <div id='feCartList'>&#160;</div>
      <div class='mt-4 pt-3 border-top js-cart-meta' id='feCheckoutArea' style='display:none'>
        <label class='fw-bold small mb-1'>FULL NAME</label>
        <input class='fe-input' id='orderName' placeholder='Your name' type='text'/>
        <label class='fw-bold small mb-1'>DELIVERY ADDRESS</label>
        <input class='fe-input' id='orderAddress' placeholder='Street, House, Casablanca' type='text'/>
      </div>
    </div>
    <div class='p-4 border-top bg-light js-cart-meta' id='feCartFooter' style='display:none'>
        <div class='h5 fw-bold d-flex justify-content-between mb-3'><span>Total:</span><span id='feFinalTotalDisplay'>0 DH</span></div>
        <button class='btn btn-dark w-100 py-3 rounded-pill fw-bold' id='btnOrderWA'>ORDER VIA WHATSAPP</button>
    </div>
  </aside>

  <aside class='fe-sidebar' id='sideWishPanel'>
    <div class='side-head' style='background:var(--fe-danger)'><h5 class='m-0 fw-bold'>Saved Items</h5><div class='fe-icon js-close-all' style='color:#fff'><i class='bi bi-x-lg'>&#160;</i></div></div>
    <div class='side-body js-wish-list' id='feWishList'>&#160;</div>
  </aside>

  <div class='fe-modal-backdrop' id='modalQuickView'>
    <div class='fe-modal-inner' onclick='event.stopPropagation()'>
      <div class='fe-modal-x js-close-all'><i class='bi bi-x-lg'>&#160;</i></div>
      
      <div class='qv-left'>
          <div class='qv-main-container'>
              <div class='qv-nav-btn qv-nav-prev js-qv-nav' data-dir='-1'><i class='bi bi-chevron-left'>&#160;</i></div>
              <img class='qv-main-img' id='feQVImg' src=''/>
              <div class='qv-nav-btn qv-nav-next js-qv-nav' data-dir='1'><i class='bi bi-chevron-right'>&#160;</i></div>
          </div>
          <div class='qv-thumb-grid' id='feQVThumbGrid'>&#160;</div>
      </div>
      
      <div class='qv-right'>
        <div id='feQVLabel'>&#160;</div>
        <h2 class='qv-title' id='feQVTitle'>-</h2>
        <div class='qv-price-row'>
            <span class='qv-old-price' id='feQVOldPrice'/>
            <span class='qv-price' id='feQVPrice'>-</span>
        </div>
        <p class='text-muted small mb-4' id='feQVDesc' style='line-height:1.6'>-</p>
        <div class='qv-qty-input'>
          <span class='qty-btn js-modal-qty' data-op='-'>&#8722;</span>
          <span class='fw-bold h5 m-0' id='feQVQtyVal'>1</span>
          <span class='qty-btn js-modal-qty' data-op='+'>+</span>
        </div>
        <div class='d-flex gap-3'>
           <button class='btn btn-dark py-3 flex-grow-1 rounded-pill fw-bold h5 m-0' id='btnModalAddToCart'>Add to Basket</button>
           <button class='fe-icon js-wish-toggle' id='btnModalWishlist' style='width:60px; height:60px; border:1px solid #eee'><i class='bi bi-heart-fill'>&#160;</i></button>
        </div>
      </div>
    </div>
  </div>

  <b:section id='BloggerLogic' showaddelement='yes'/>

  <script type='text/javascript'>
  //<![CDATA[
  document.addEventListener("DOMContentLoaded", function() {
    (function(){
      const FE = {
        config: { waPhone: "212600000000", storeName: "EVENT SUSHI" },
        catalog: [
          { id: "p1", title: "Salmon Lovers Box", price: 120, oldPrice: 155, label: "Sale", cat: "Special Rolls", 
            imgs: ["https://images.unsplash.com/photo-1579871494447-9811cf80d66c?w=600", "https://images.unsplash.com/photo-1553621042-f6e147245754?w=600", "https://images.unsplash.com/photo-1593560708920-61dd98c46a4e?w=600"], 
            desc: "Premium selection of 12 pieces including Salmon Nigiri and rolls.", expiry: new Date().getTime() + 7200000 },
          { id: "p2", title: "Dragon Platter", price: 155, label: "Hot", cat: "Special Rolls", 
            imgs: ["https://images.unsplash.com/photo-1553621042-f6e147245754?w=600", "https://images.unsplash.com/photo-1593560708920-61dd98c46a4e?w=600"], 
            desc: "Signature tempura shrimp roll topped with grilled unagi." },
          { id: "p3", title: "Spicy Tuna Nigiri", price: 45, oldPrice: 60, cat: "Nigiri", label: "Sale",
            imgs: ["https://images.unsplash.com/photo-1593560708920-61dd98c46a4e?w=600"], 
            desc: "Hand-pressed sushi rice topped with spicy tuna.", expiry: new Date().getTime() + 3600000 }
        ],
        state: { filter: "all", cart: JSON.parse(localStorage.getItem("fe_cart") || "[]"), wish: JSON.parse(localStorage.getItem("fe_wish") || "[]"), qvId: null, qvQty: 1, qvImgIdx: 0 },
        $: id => document.getElementById(id),
        $$: s => document.querySelectorAll(s),
        save: function() { localStorage.setItem("fe_cart",JSON.stringify(this.state.cart)); localStorage.setItem("fe_wish",JSON.stringify(this.state.wish)); },
        
        openSidebar: function(id) { this.$(id).classList.add("open"); const b = this.$("feSideBackdrop"); b.style.display="block"; setTimeout(()=>b.style.opacity="1",10); },
        closeAll: function() { this.$$(".fe-sidebar").forEach(s=>s.classList.remove("open")); this.$$(".fe-modal-backdrop").forEach(m=>m.style.display="none"); const b = this.$("feSideBackdrop"); b.style.opacity="0"; setTimeout(()=>b.style.display="none",300); },

        renderMenu: function() {
          const items = this.state.filter === "all" ? this.catalog : this.catalog.filter(p => p.cat === this.state.filter);
          this.$("feMenuGrid").innerHTML = items.map(p => `
            <div class="fe-card reveal js-card" data-id="${p.id}">
              ${p.label ? `<span class="product-label label-${p.label.toLowerCase()}">${p.label}</span>` : ''}
              ${p.expiry ? `<div class="flash-timer js-timer" data-end="${p.expiry}"><span class="timer-val">--:--</span></div>` : ''}
              <div class="card-img-box"><img src="${p.imgs[0]}"/><div class="card-actions">
                  <button class="grid-btn grid-btn-cart js-quick-add" data-id="${p.id}"><i class="bi bi-bag-plus">&#160;</i> Add</button>
                  <button class="grid-btn grid-btn-wish js-wish-toggle ${this.state.wish.includes(p.id)?'active':''}" data-id="${p.id}"><i class="bi bi-heart-fill">&#160;</i></button>
              </div></div>
              <div class="card-body"><h6 class="fw-bold m-0">${p.title}</h6><div class="mt-2">${p.oldPrice?`<small class='text-decoration-line-through text-muted me-2'>${p.oldPrice} DH</small>`:''}<b>${p.price} DH</b></div></div>
            </div>`).join('');
          this.initTimers();
          const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting) e.target.classList.add("active");}),{threshold:0.1});
          this.$$(".reveal").forEach(el=>obs.observe(el));
        },

        initTimers: function() {
            const update = () => {
                this.$$(".js-timer").forEach(t => {
                    const diff = parseInt(t.dataset.end) - new Date().getTime();
                    if(diff <= 0) { t.innerHTML = "EXPIRED"; } else {
                        const m = Math.floor((diff % 3600000) / 60000), s = Math.floor((diff % 60000) / 1000);
                        t.querySelector(".timer-val").textContent = `${m}m ${s}s left`;
                    }
                });
            };
            setInterval(update, 1000);
        },

        renderCart: function() {
          const valid = this.state.cart.filter(i=>this.catalog.find(p=>p.id===i.id));
          this.$("feCartList").innerHTML = valid.map(i => {
            let p=this.catalog.find(x=>x.id===i.id);
            return `<div class="row-item"><img src="${p.imgs[0]}"/><div><h6>${p.title}</h6><small>${p.price} DH x ${i.qty}</small></div><div class="ms-auto js-del-cart" data-id="${i.id}" style="cursor:pointer; color:red; font-weight:900">DEL</div></div>`;
          }).join('');
          this.$("feFinalTotalDisplay").textContent = valid.reduce((s,i)=>s+(this.catalog.find(x=>x.id===i.id).price*i.qty),0) + " DH";
          this.$$(".js-cart-meta").forEach(el=>el.style.display = valid.length ? "block" : "none");
          this.$("cartBadge").textContent = valid.reduce((s,i)=>s+i.qty,0);
          this.save();
        },

        renderWish: function() {
          const valid = this.state.wish.filter(id=>this.catalog.find(p=>p.id===id));
          this.$("feWishList").innerHTML = valid.map(id => {
            let p=this.catalog.find(x=>x.id===id);
            return `<div class="row-item"><img src="${p.imgs[0]}"/><div><h6>${p.title}</h6><button class="btn btn-sm btn-dark js-quick-add" data-id="${p.id}">+ Basket</button></div><div class="ms-auto js-del-wish" data-id="${id}" style="cursor:pointer; font-weight:900">X</div></div>`;
          }).join('');
          this.$("wishBadge").textContent = valid.length;
          this.save();
        },

        updateQVImage: function(idx) {
            const p = this.catalog.find(x => x.id === this.state.qvId);
            if(!p || !p.imgs[idx]) return;
            this.state.qvImgIdx = idx;
            this.$("feQVImg").src = p.imgs[idx];
            this.$$(".qv-thumb").forEach((th, i) => th.classList.toggle("active", i === idx));
        },

        bindEvents: function() {
          const self = this;
          document.addEventListener("click", e => {
            const t = e.target;
            if (t.closest("#cartToggle")) { self.openSidebar("sideCartPanel"); return; }
            if (t.closest("#wishToggle")) { self.openSidebar("sideWishPanel"); return; }
            if (t.closest(".cat-item")) {
                self.$$(".cat-item").forEach(c => c.classList.remove("active"));
                t.closest(".cat-item").classList.add("active");
                self.state.filter = t.closest(".cat-item").dataset.cat;
                self.renderMenu(); return;
            }
            if (t.closest(".js-close-all") || t.id === "feSideBackdrop" || t.id === "modalQuickView") { self.closeAll(); return; }

            // STEP 14 GALLERY ACTIONS
            if (t.closest(".js-qv-nav")) {
                const dir = parseInt(t.closest(".js-qv-nav").dataset.dir);
                const p = self.catalog.find(x => x.id === self.state.qvId);
                let nextIdx = self.state.qvImgIdx + dir;
                if(nextIdx < 0) nextIdx = p.imgs.length - 1;
                if(nextIdx >= p.imgs.length) nextIdx = 0;
                self.updateQVImage(nextIdx);
                return;
            }
            if (t.closest(".js-thumb")) {
                self.updateQVImage(Array.from(self.$$(".qv-thumb")).indexOf(t.closest(".js-thumb")));
                return;
            }

            const card = t.closest(".js-card");
            if (card && !t.closest(".card-actions")) {
              const p = self.catalog.find(x=>x.id===card.dataset.id);
              self.state.qvId=p.id; self.state.qvQty=1; self.state.qvImgIdx=0;
              self.$("feQVImg").src=p.imgs[0]; self.$("feQVTitle").textContent=p.title; self.$("feQVPrice").textContent=p.price + " DH";
              self.$("feQVOldPrice").textContent = p.oldPrice ? p.oldPrice + " DH" : "";
              self.$("feQVDesc").textContent=p.desc; self.$("feQVQtyVal").textContent="1";
              self.$("feQVThumbGrid").innerHTML = p.imgs.length > 1 ? p.imgs.map((src, i) => `<img class="qv-thumb js-thumb ${i===0?'active':''}" src="${src}"/>`).join('') : '';
              self.$("btnModalWishlist").classList.toggle("active", self.state.wish.includes(p.id));
              self.$("modalQuickView").style.display="flex";
              return;
            }

            if (t.closest(".js-modal-qty")) {
              const op = t.closest(".js-modal-qty").dataset.op;
              op === "+" ? self.state.qvQty++ : (self.state.qvQty>1 ? self.state.qvQty-- : null);
              self.$("feQVQtyVal").textContent = self.state.qvQty;
              return;
            }

            if (t.id==="btnModalAddToCart" || t.closest(".js-quick-add")) {
              const btn = t.closest(".js-quick-add");
              const id = btn ? btn.dataset.id : self.state.qvId;
              const qty = btn ? 1 : self.state.qvQty;
              const ex=self.state.cart.find(x=>x.id===id);
              ex ? ex.qty+=qty : self.state.cart.push({id, qty});
              self.closeAll(); self.renderCart(); self.openSidebar("sideCartPanel");
              return;
            }

            if (t.closest(".js-wish-toggle")) {
              const id = t.closest(".js-wish-toggle").dataset.id || self.state.qvId;
              const idx = self.state.wish.indexOf(id);
              idx > -1 ? self.state.wish.splice(idx,1) : self.state.wish.push(id);
              self.renderWish(); self.renderMenu();
              if(self.$("btnModalWishlist")) self.$("btnModalWishlist").classList.toggle("active", self.state.wish.includes(self.state.qvId));
              return;
            }

            if (t.closest(".js-del-cart")) { self.state.cart = self.state.cart.filter(x => x.id !== t.closest(".js-del-cart").dataset.id); self.renderCart(); return; }
            if (t.closest(".js-del-wish")) { self.state.wish = self.state.wish.filter(x => x !== t.closest(".js-del-wish").dataset.id); self.renderWish(); self.renderMenu(); return; }

            if (t.id === "btnOrderWA") {
                const name = self.$("orderName").value.trim();
                const addr = self.$("orderAddress").value.trim();
                if(!name || !addr) { alert("Fill Name and Address"); return; }
                let msg = `🍣 ORDER\n👤 Name: ${name}\n📍 Addr: ${addr}\n------------------\n`;
                self.state.cart.forEach(item => {
                    let p = self.catalog.find(x => x.id === item.id);
                    msg += `• ${item.qty}x ${p.title} (${p.price * item.qty} DH)\n`;
                });
                window.open(`https://wa.me/${self.config.waPhone}?text=${encodeURIComponent(msg)}`);
            }
          }, true);
        },
        init: function() { this.renderMenu(); this.renderCart(); this.renderWish(); this.bindEvents(); }
      };
      FE.init();
    })();
  });
  //]]>
  </script>
</body>
</html>
